// variables example

#include <stdio.h>

int main()
{
    int a = 1;
    float b = 3.5;
    double c = 2.71;
    float sum;

    sum = a + b + c; // we sum the variables

    printf("The sum of a, b and c is: %f\n", sum); // %f is used to print floating point numbers
    return 0;
}
