#include <stdio.h>
#include <stdbool.h>
// on this case use the stdbool.h library to use the bool type
int main()
{
    bool flag = true;
    printf("%d\n", flag); // imprime 1
    flag = false;
    printf("%d\n", flag); // imprime 0
    return 0;
}
